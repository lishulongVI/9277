package main

func main() {
	// fmt.Println(a, b, c, d, e, f, g, h, iii, j, x, y, z, w, v)

	// display()
	// fmt.Printf("The first element is %d\n", arr[0]) // 获取数据，返回42
	// fmt.Printf("The last element is %d\n", arr[9])  //返回未赋值的最后一个元素，默认返回0

	// slice()

	// printX(10)

	// computer()
	for_()

	switch_(3)

}
